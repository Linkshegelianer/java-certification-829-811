/*
Enum is a fixed set of constants.
- Simple enum - consists of just a list of values.
 */
package enums;

public enum SimpleEnum {

    WINTER, SPRING, SUMMER, FALL // enum values are static final constants, semicolon optional for simple enums
}